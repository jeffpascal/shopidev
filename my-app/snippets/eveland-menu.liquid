<nav class="navbar navbar-expand-lg navbar-light p-0 text-center justify-content-between">
  <div class="collapse navbar-collapse">
    <ul class="navbar-nav">
      {% assign main_menu = section.settings.main_menu_store %}
      {% for link in linklists[main_menu].links %}

      {% assign item = link.title | split: '[' | first | downcase %}
      {% assign child_list_handle = link.title | handleize %}

      {% capture megamenu_1 %}megamenu_s1_parent{% endcapture %}
      {% capture megamenu_2 %}megamenu_s2_parent{% endcapture %}
      {% capture megamenu_3 %}megamenu_s3_parent{% endcapture %}
      {% capture megamenu_4 %}megamenu_s4_parent{% endcapture %}
      {% capture megamenu_5 %}megamenu_s5_parent{% endcapture %}

      {% if settings[megamenu_1] == item or settings[megamenu_2] == item  or settings[megamenu_3] == item or settings[megamenu_4] == item  or settings[megamenu_5] == item %}
      {% for i in (1..5) %}

      {% capture megamenu %}megamenu_s{{i}}_parent{% endcapture %}
      {% capture _mega_style_select %}ms{{i}}_select{% endcapture %}
      {% capture _mega_lcol_img_up %}ms{{i}}_lc_img{% endcapture %}
      {% capture _mega_lcol_img_url %}ms{{i}}_lc_img_url{% endcapture %}
      {% capture _mega_bg_img %}megamenu{{i}}_bg_img{% endcapture %}
      


      {% if settings[megamenu] == item %}
      {% if settings[_mega_style_select] == "m_s1" %}
      <li class="nav-item dropdown">
        {% if link.title contains '[' %}
        {%- assign label  = link.title | split: '[' | last  | split: ']' | first -%}  
        {%- assign bgc  = link.title | split: '(' | last  | split: ')' | first -%}
        
        <a href="{{ link.url }}" title="{{link.title | split: '[' | first }}" class="delay03  relative nav-link menu_lv1 ">{{link.title | split: '[' | first }}
          <span class="absolute label_menu label_menu-{{link.title | handleize}}-{{forloop.index}}" {% if link.title contains '(' %}style="background-color: {{link.title | split: '(' | last  | split: ')' | first}};"{% endif %}><span>{{link.title | split: '[' | last  | split: ']' | first}}</span></span>
          <style>
            .label_menu-{{link.title | handleize}}-{{forloop.index}}:before {
                border-top: 4px solid {{link.title | split: '(' | last  | split: ')' | first}};
              }            
          </style>
        </a>
        {% else %}
        <a href="{{ link.url }}" title="{{link.title | split: '[' | first }}" class="delay03 relative nav-link  menu_lv1 ">{{link.title | split: '[' | first }}</a>
        {% endif %}
        <div class="dropdown-menu list-woman shop show" >
          
          <div class="row">
            {% for childlink in linklists[child_list_handle].links %}
            {% assign grand_child_list_handle = childlink.title | handle %}

            {% if linklists[grand_child_list_handle].links != blank %}
            <div class="col-lg-6">
              <div class="list-clothing">
                <div class="title">
                  <h5>
                    {% if childlink.title contains '[' %}
                    {%- assign label  = link.title | split: '[' | last  | split: ']' | first -%}  
                    {%- assign bgc  = link.title | split: '(' | last  | split: ')' | first -%}
                    <a href="{{ childlink.url }}" title="{{child_link.title | split: '[' | first }}" class="delay03  relative">{{childlink.title | split: '[' | first }}
                      
                      <span class="absolute label_menu label_menu-{{childlink.title | handleize}}-{{forloop.index}}" {% if childlink.title contains '(' %}style="background-color: {{childlink.title | split: '(' | last  | split: ')' | first}};"{% endif %}><span>{{childlink.title | split: '[' | last  | split: ']' | first}}</span></span>

                      <style>
                        .label_menu-{{childlink.title | handleize}}-{{forloop.index}}:before {
                          border-top: 4px solid {{childlink.title | split: '(' | last  | split: ')' | first}};
                        }
                      </style>
                      
                    </a>
                    {%else%}
                    <a href="{{ childlink.url }}" title="{{childlink.title | split: '[' | first }}" class="delay03 relative ">{{childlink.title | split: '[' | first }}</a>
                    {%endif%}
                    
                  </h5>
                </div>
                <ul class="mb-0 list-unstyled">
                  {% for grandchildlink in linklists[grand_child_list_handle].links %}
                  <li>

                    {% if grandchildlink.title contains '[' %}
                    {%- assign label  = grandchildlink.title | split: '[' | last  | split: ']' | first -%}  
                    {%- assign bgc  = grandchildlink.title | split: '(' | last  | split: ')' | first -%}
                    <a href="{{ grandchildlink.url }}" title="{{grandchildlink.title | split: '[' | first }}" class="relative delay03 nav-link  menu_lv1 ">{{grandchildlink.title | split: '[' | first }}
                      <span class="absolute label_menu label_menu-{{grandchildlink.title | handleize}}-{{forloop.index}}" {% if grandchildlink.title contains '(' %}style="background-color: {{grandchildlink.title | split: '(' | last  | split: ')' | first}};"{% endif %}><span>{{grandchildlink.title | split: '[' | last  | split: ']' | first}}</span></span>
                    
                    <style>
                        .label_menu-{{grandchildlink.title | handleize}}-{{forloop.index}}:before {
                          border-top: 4px solid {{grandchildlink.title | split: '(' | last  | split: ')' | first}};
                        }
                      </style>
                    </a>
                    {% else %}
                    <a href="{{ grandchildlink.url }}" title="{{grandchildlink.title | split: '[' | first }}" class="delay03 relative nav-link menu_lv1 ">{{grandchildlink.title | split: '[' | first }}</a>
                    {% endif %}
                  </li>
                  {% endfor %}
                </ul>
              </div>
            </div>
            {% else %}
            <div class="col-lg-6">
              <div class="list-clothing">
                <div class="title">
                  <h5><a href="{{ childlink.url }}">{{ childlink.title }}</a></h5>
                </div>
              </div>
            </div>
            {% endif %}
            {% endfor %}
          </div>
          
        </div>
      </li>

      {% elsif settings[_mega_style_select] == "m_s2" %}
      <li class="nav-item dropdown">
        {% if link.title contains '[' %}
        {%- assign label  = link.title | split: '[' | last  | split: ']' | first -%}  
        {%- assign bgc  = link.title | split: '(' | last  | split: ')' | first -%}
        <a href="{{ link.url }}" title="{{link.title | split: '[' | first }}" class="delay03 relative  nav-link menu_lv1 ">{{link.title | split: '[' | first }}
          <span class="absolute label_menu label_menu-{{link.title | handleize}}-{{forloop.index}}" {% if link.title contains '(' %}style="background-color: {{link.title | split: '(' | last  | split: ')' | first}};"{% endif %}><span>{{link.title | split: '[' | last  | split: ']' | first}}</span></span>
          <style>
            .label_menu-{{link.title | handleize}}-{{forloop.index}}:before {
              border-top: 4px solid {{link.title | split: '(' | last  | split: ')' | first}};
            }
          </style>
        </a>
        {% else %}
        <a href="{{ link.url }}" title="{{link.title | split: '[' | first }}" class="delay03 relative nav-link  menu_lv1 ">{{link.title | split: '[' | first }}</a>
        {% endif %}
        <div class="dropdown-menu list-woman featured show">
          <div class="row">
            <div class="col-lg-9">
              <div class="row">
                {% for childlink in linklists[child_list_handle].links %}
                {% assign grand_child_list_handle = childlink.title | handle %}

                {% if linklists[grand_child_list_handle].links != blank %}
                <div class="col-lg-4">
                  <div class="list-clothing">
                    <div class="title">
                      <h5>
                        {% if childlink.title contains '[' %}
                        {%- assign label  = link.title | split: '[' | last  | split: ']' | first -%}  
                        {%- assign bgc  = link.title | split: '(' | last  | split: ')' | first -%}
                        <a href="{{ childlink.url }}" title="{{child_link.title | split: '[' | first }}" class="delay03  relative">{{childlink.title | split: '[' | first }}
                          <span class="absolute label_menu label_menu-{{childlink.title | handleize}}-{{forloop.index}}" {% if childlink.title contains '(' %}style="background-color: {{childlink.title | split: '(' | last  | split: ')' | first}};"{% endif %}><span>{{childlink.title | split: '[' | last  | split: ']' | first}}</span></span>
                          <style>
                            .label_menu-{{childlink.title | handleize}}-{{forloop.index}}:before {
                              border-top: 4px solid {{childlink.title | split: '(' | last  | split: ')' | first}};
                            }
                          </style>
                        
                        </a>
                        {%else%}
                        <a href="{{ childlink.url }}" title="{{childlink.title | split: '[' | first }}" class="delay03 relative ">{{childlink.title | split: '[' | first }}</a>
                        {%endif%}
                        
                      </h5>
                    </div>
                    <ul class="mb-0 list-unstyled">
                      {% for grandchildlink in linklists[grand_child_list_handle].links %}
                      <li>

                        {% if grandchildlink.title contains '[' %}
                        {%- assign label  = grandchildlink.title | split: '[' | last  | split: ']' | first -%}  
                        {%- assign bgc  = grandchildlink.title | split: '(' | last  | split: ')' | first -%}
                        <a href="{{ grandchildlink.url }}" title="{{grandchildlink.title | split: '[' | first }}" class="relative delay03 nav-link  menu_lv1 }">{{grandchildlink.title | split: '[' | first }}
                          <span class="absolute label_menu label_menu-{{grandchildlink.title | handleize}}-{{forloop.index}}" {% if grandchildlink.title contains '(' %}style="background-color: {{grandchildlink.title | split: '(' | last  | split: ')' | first}};"{% endif %}><span>{{grandchildlink.title | split: '[' | last  | split: ']' | first}}</span></span>
                        
                          <style>
                            .label_menu-{{grandchildlink.title | handleize}}-{{forloop.index}}:before {
                              border-top: 4px solid {{grandchildlink.title | split: '(' | last  | split: ')' | first}};
                            }
                          </style>
                        </a>
                        {% else %}
                        <a href="{{ grandchildlink.url }}" title="{{grandchildlink.title | split: '[' | first }}" class="delay03 relative nav-link menu_lv1 ">{{grandchildlink.title | split: '[' | first }}</a>
                        {% endif %}
                      </li>
                      {% endfor %}
                    </ul>
                  </div>
                </div>
                {% else %}
                <div class="col-lg-4">
                  <div class="list-clothing">
                    <div class="title">
                      <h5><a href="{{ childlink.url }}">{{ childlink.title }}</a></h5>
                    </div>
                  </div>
                </div>
                {% endif %}
                {% endfor %}
              </div>
            </div>
            <div class="col-lg-3">
              <div class="picture">
                <a href="{{settings[_mega_lcol_img_url]}}">
                  {% if settings[_mega_lcol_img_up] == blank %}
                  {{ 'image' | placeholder_svg_tag: 'placeholder-svg' }}
                  {% else %}
                  <img src="{{settings[_mega_lcol_img_up] | img_url: "master"}}" alt="banner-megamenu">
                  {% endif %}
                </a>
              </div>
            </div>
          </div>
        </div>
      </li>

      {% elsif settings[_mega_style_select] == "m_s3" %}
      <li class="nav-item dropdown">
        {% if link.title contains '[' %}
        {%- assign label  = link.title | split: '[' | last  | split: ']' | first -%}  
        {%- assign bgc  = link.title | split: '(' | last  | split: ')' | first -%}
        <a href="{{ link.url }}" title="{{link.title | split: '[' | first }}" class="delay03 relative nav-link menu_lv1 ">{{link.title | split: '[' | first }}
          <span class="absolute label_menu  label_menu-{{link.title | handleize}}-{{forloop.index}}" {% if link.title contains '(' %}style="background-color: {{link.title | split: '(' | last  | split: ')' | first}};"{% endif %}><span>{{link.title | split: '[' | last  | split: ']' | first}}</span></span>
        
          <style>
            .label_menu-{{link.title | handleize}}-{{forloop.index}}:before {
              border-top: 4px solid {{link.title | split: '(' | last  | split: ')' | first}};
            }
          </style>
        
        </a>
        {% else %}
        <a href="{{ link.url }}" title="{{link.title | split: '[' | first }}" class="delay03 relative nav-link menu_lv1 ">{{link.title | split: '[' | first }}</a>
        {% endif %}
        <div class="dropdown-menu list-woman show style_3" style="{% if settings[_mega_bg_img] != blank %}background-image: url({{settings[_mega_bg_img] | img_url:'master'}});{% endif %}">
          <div class="row">
            {% for childlink in linklists[child_list_handle].links %}
            {% assign grand_child_list_handle = childlink.title | handle %}

            {% if linklists[grand_child_list_handle].links != blank %}
            <div class="col-lg-4">
              <div class="list-clothing">
                <div class="title">
                  <h5>
                    {% if childlink.title contains '[' %}
                    {%- assign label  = link.title | split: '[' | last  | split: ']' | first -%}  
                    {%- assign bgc  = link.title | split: '(' | last  | split: ')' | first -%}
                    <a href="{{ childlink.url }}" title="{{child_link.title | split: '[' | first }}" class="delay03  relative">{{childlink.title | split: '[' | first }}
                      
                      <span class="absolute label_menu label_menu-{{childlink.title | handleize}}-{{forloop.index}}" {% if childlink.title contains '(' %}style="background-color: {{childlink.title | split: '(' | last  | split: ')' | first}};"{% endif %}><span>{{childlink.title | split: '[' | last  | split: ']' | first}}</span></span>

                      <style>
                        .label_menu-{{childlink.title | handleize}}-{{forloop.index}}:before {
                          border-top: 4px solid {{childlink.title | split: '(' | last  | split: ')' | first}};
                        }
                      </style>
                      
                    </a>
                    {%else%}
                    <a href="{{ childlink.url }}" title="{{childlink.title | split: '[' | first }}" class="delay03 relative ">{{childlink.title | split: '[' | first }}</a>
                    {%endif%}
                    
                  </h5>
                </div>
                <ul class="mb-0 list-unstyled">
                  {% for grandchildlink in linklists[grand_child_list_handle].links %}
                  <li>

                    {% if grandchildlink.title contains '[' %}
                    {%- assign label  = grandchildlink.title | split: '[' | last  | split: ']' | first -%}  
                    {%- assign bgc  = grandchildlink.title | split: '(' | last  | split: ')' | first -%}
                    <a href="{{ grandchildlink.url }}" title="{{grandchildlink.title | split: '[' | first }}" class="relative delay03 nav-link  menu_lv1 ">{{grandchildlink.title | split: '[' | first }}
                      <span class="absolute label_menu label_menu-{{grandchildlink.title | handleize}}-{{forloop.index}}" {% if grandchildlink.title contains '(' %}style="background-color: {{grandchildlink.title | split: '(' | last  | split: ')' | first}};"{% endif %}><span>{{grandchildlink.title | split: '[' | last  | split: ']' | first}}</span></span>
                    
                    <style>
                        .label_menu-{{grandchildlink.title | handleize}}-{{forloop.index}}:before {
                          border-top: 4px solid {{grandchildlink.title | split: '(' | last  | split: ')' | first}};
                        }
                      </style>
                    </a>
                    {% else %}
                    <a href="{{ grandchildlink.url }}" title="{{grandchildlink.title | split: '[' | first }}" class="delay03 relative nav-link menu_lv1 ">{{grandchildlink.title | split: '[' | first }}</a>
                    {% endif %}
                  </li>
                  {% endfor %}
                </ul>
              </div>
            </div>
            {% else %}
            <div class="col-lg-4">
              <div class="list-clothing">
                <div class="title">
                  <h5><a href="{{ childlink.url }}">{{ childlink.title }}</a></h5>
                </div>
              </div>
            </div>
            {% endif %}
            {% endfor %}
          </div>          
        </div>
      </li>

      {% elsif settings[_mega_style_select] == "m_s4" %}
      <li class="nav-item dropdown">
        {% if link.title contains '[' %}
        {%- assign label  = link.title | split: '[' | last  | split: ']' | first -%}  
        {%- assign bgc  = link.title | split: '(' | last  | split: ')' | first -%}
        <a href="{{ link.url }}" title="{{link.title | split: '[' | first }}" class="delay03 relative nav-link menu_lv1">{{link.title | split: '[' | first }}
          <span class="absolute label_menu label_menu-{{link.title | handleize}}-{{forloop.index}}" {% if link.title contains '(' %}style="background-color: {{link.title | split: '(' | last  | split: ')' | first}};"{% endif %}><span>{{link.title | split: '[' | last  | split: ']' | first}}</span></span>
        
          <style>
            .label_menu-{{link.title | handleize}}-{{forloop.index}}:before {
              border-top: 4px solid {{link.title | split: '(' | last  | split: ')' | first}};
            }
          </style>
        
        </a>
        {% else %}
        <a href="{{ link.url }}" title="{{link.title | split: '[' | first }}" class="delay03 relative nav-link menu_lv1 ">{{link.title | split: '[' | first }}</a>
        {% endif %}
        <div class="dropdown-menu list-home show " style="{% if settings[_mega_bg_img] != blank %}background-image: url({{settings[_mega_bg_img] | img_url:'master'}});{% endif %}">
          <div class="picture_menu">
            <div class="row">
              {% for j in (1..10) %}
              {% capture mega_title %}megamenu{{i}}_block{{j}}_title{% endcapture %}
              {% capture img_style4 %}ms{{i}}_s4_img{{j}}{% endcapture %}
              {% capture img_url_style4 %}ms{{i}}_s4_url{{j}}{% endcapture %}
              {% if settings[img_style4] != blank %}
              <div class="col-lg-4">
                <div class="eveland-demolink">
                  <a href="{{settings[img_url_style4]}}" title="{{settings[mega_title]}}">
                    <img src="{{settings[img_style4]|img_url:'master'}}" class="img-fluid" alt="banner">
                  </a>                  
                  {% if settings[mega_title] != blank %}<h5 class="mb-0 title">{{settings[mega_title]}}</h5>{% endif %}
                </div>
              </div>
              {% endif %}
              {% endfor %}

            </div>
          </div>
        </div>
      </li>
      {% endif %}

      {% endif %}
      {% endfor %}

      {% else %}
      {% if linklists[child_list_handle].links != blank %}
      <li class="nav-item dropdown">
        {% if link.title contains '[' %}
        {%- assign label  = link.title | split: '[' | last  | split: ']' | first -%}  
        {%- assign bgc  = link.title | split: '(' | last  | split: ')' | first -%}
        <a href="{{ link.url }}" title="{{link.title | split: '[' | first }}" class="delay03  nav-link menu_lv1 ">{{link.title | split: '[' | first }}
          <span class="absolute label_menu label_menu-{{link.title | handleize}}-{{forloop.index}}" {% if link.title contains '(' %}style="background-color: {{link.title | split: '(' | last  | split: ')' | first}};"{% endif %}><span>{{link.title | split: '[' | last  | split: ']' | first}}</span></span>
          <style>
            .label_menu-{{link.title | handleize}}-{{forloop.index}}:before {
              border-top: 4px solid {{link.title | split: '(' | last  | split: ')' | first}};
            }
          </style>
        </a>
        {% else %}
        <a href="{{ link.url }}" title="{{link.title | split: '[' | first }}" class="delay03 nav-link  menu_lv1 ">{{link.title | split: '[' | first }}</a>
        {% endif %}
        <div class="dropdown-menu list-woman page show">
          <div class="col-lg-12">
            <div class="list-clothing">
              <ul class="mb-0 list-unstyled">
                {% for childlink in linklists[child_list_handle].links %}
                <li>

                  {% if childlink.title contains '[' %}
                  {%- assign label  = childlink.title | split: '[' | last  | split: ']' | first -%}  
                  {%- assign bgc  = childlink.title | split: '(' | last  | split: ')' | first -%}
                  <a href="{{ childlink.url }}" title="{{childlink.title | split: '[' | first }}" class="relative delay03  nav-link menu_lv1 ">{{childlink.title | split: '[' | first }}
                    <span class="absolute label_menu label_menu-{{childlink.title | handleize}}-{{forloop.index}}" {% if childlink.title contains '(' %}style="background-color: {{childlink.title | split: '(' | last  | split: ')' | first}};"{% endif %}><span>{{childlink.title | split: '[' | last  | split: ']' | first}}</span></span>
                  
                    <style>
                      .label_menu-{{childlink.title | handleize}}-{{forloop.index}}:before {
                        border-top: 4px solid {{childlink.title | split: '(' | last  | split: ')' | first}};
                      }
                    </style>
                  </a>
                  {% else %}
                  <a href="{{ childlink.url }}" title="{{childlink.title | split: '[' | first }}" class="delay03 relative nav-link  menu_lv1 ">{{childlink.title | split: '[' | first }}</a>
                  {% endif %}
                </li>
                {% endfor %}
              </ul>
            </div>
          </div>
        </div>
      </li>
      {% else %}
      <li class="nav-item dropdown">
        {% if link.title contains '[' %}
        {%- assign label  = link.title | split: '[' | last  | split: ']' | first -%}  
        {%- assign bgc  = link.title | split: '(' | last  | split: ')' | first -%}
        <a href="{{ link.url }}" title="{{link.title | split: '[' | first }}" class="delay03 relative menu_lv1 nav-link">{{link.title | split: '[' | first }}
          <span class="absolute label_menu label_menu-{{link.title | handleize}}-{{forloop.index}}" {% if link.title contains '(' %}style="background-color: {{link.title | split: '(' | last  | split: ')' | first}};"{% endif %}><span>{{link.title | split: '[' | last  | split: ']' | first}}</span></span>
          <style>

            .label_menu-{{link.title | handleize}}-{{forloop.index}}:before {
                border-top: 4px solid {{link.title | split: '(' | last  | split: ')' | first}};
              }
            }

          </style>
        </a>
        {% else %}
        <a href="{{ link.url }}" title="{{link.title | split: '[' | first }}" class="delay03 relative menu_lv1 nav-link">{{link.title | split: '[' | first }}</a>
        {% endif %}
      </li>
      {% endif %}
      {% endif %}

      {% endfor %}
    </ul>
  </div>
</nav>