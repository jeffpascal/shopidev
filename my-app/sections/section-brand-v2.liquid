<div data-section-id="{{ section.id }}" data-section-type="section-brand-v2" style="{% if section.settings.margin_top != blank %}margin-top: {{section.settings.margin_top}}px;{%endif%} {% if section.settings.margin_bottom != blank %}margin-bottom: {{section.settings.margin_bottom}}px;{% endif %} {% if section.settings.sections_layout == "dark" %}background: {{section.settings.sections_bg_color}};{% endif %}">
  <div class="section-brand-v2">
    <div class="container container-v1">
      <div class="bg_section" style="background-image: url({% if section.settings.backgroud_image != blank %}{{section.settings.backgroud_image|img_url:'master'}}{% else %}//placehold.it/1640x463{% endif %})">
      	<div class="title-brand text-center">
          {% if section.settings.title-brand != blank %}
          	<h3 class="mb-0">{{section.settings.title-brand}}</h3>
          {% endif %}
        </div>
        <div class="jsbrand_list_v2 box-bottom-brand">
        {% if section.blocks.size > 0 %}
        {% for block in section.blocks %}
        {% if block.type == 'brand' %}
        <div class="item">
          <a href="{{block.settings.brand_url}}">
            {% if block.settings.brand_img != blank %}
            <img src="{{block.settings.brand_img|img_url:'master'}}" class="img-fluid" alt="bland">
            {% else %}
            <img src="https://placehold.it/165x90" class="img-fluid" alt="bland">
            {% endif %}
          </a>
        </div>
        {% endif %}
        {% endfor %}
        {% endif %} 
      </div>
      </div>
    </div>
  </div>
</div>
{% schema %}
  {
  "name": "Brand v2",
  "class": "index-section",
  "settings": [
	{
      "type": "image_picker",
      "id": "backgroud_image",
      "info": "1640 x 463px recommended",
      "label": "background images brand"
    },
	{
      "type": "text",
      "id": "title-brand",
      "label": "Title brand",
      "default": "Top Brand"
    },
	{
      "type": "header",
      "content": "Section Space"
    },
    {
      "type": "text",
      "id": "margin_top",
      "label": "Margin Top",
      "info": "Defined in pixels. Do not add the 'px' unit."
    },
    {
      "type": "text",
      "id": "margin_bottom",
      "label": "Margin Bottom",
      "info": "Defined in pixels. Do not add the 'px' unit."
    }
	
  ],
  "blocks": [
    {
      "type": "brand",
      "name": "Brand V2",
      "settings": [
        {
          "type": "image_picker",
          "id": "brand_img",
          "info": "165 x 90px recommended",
          "label": "Brand 1"
        },
        {
          "type": "url",
          "id": "brand_url",
          "label": "URL brand ",
          "default": "/collections/all"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Brand v2",
      "category": "HOME 3",
	  "blocks": [
        {
          "type": "brand",
          "settings": {
          }
        },
        {
          "type": "brand",
          "settings": {
          }
        },
        {
          "type": "brand",
          "settings": {
          }
        },
		{
          "type": "brand",
          "settings": {
          }
        },
		{
          "type": "brand",
          "settings": {
          }
        },
        {
          "type": "brand",
          "settings": {
          }
        }
      ]
    }
  ]
}
{% endschema %}


{% stylesheet %}
{% endstylesheet %}

{% javascript %}
{% endjavascript %}
